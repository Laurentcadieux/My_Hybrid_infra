# Ansible inventory — generated from Terraform outputs
# Run: terraform output -json > inventory/terraform_outputs.json
# Then: python3 inventory/generate.py

all:
  children:
    do_edge:
      hosts:
        nginx:
          ansible_host: 192.241.155.248
          ansible_user: root
          ansible_ssh_private_key_file: ../../keys/hybrid-infra-admin
    proxmox_vms:
      hosts:
        vpn_gw:
          ansible_host: 192.168.0.106
          ansible_user: ubuntu
          ansible_ssh_private_key_file: ../../keys/hybrid-infra-admin
          role: vpn-gateway
        web_cv:
          ansible_host: 192.168.0.105
          ansible_user: ubuntu
          ansible_ssh_private_key_file: ../../keys/hybrid-infra-admin
          role: web-cv
          site_repo: https://github.com/Laurentcadieux/LaurentCadieuxDOTonline.git
          site_domain: laurentcadieux.online
